import {Part} from '../lib/types'
export default function BuildList({items,onRemove,onClear}:{items:Part[];onRemove:(id:number)=>void;onClear:()=>void}){const total=items.reduce((a,b)=>a+b.current_price,0);return <aside className='sticky top-4 bg-slate-900 p-4 rounded border border-slate-800'><h3 className='font-semibold'>Build List</h3>{items.length===0&&<p className='text-sm text-slate-400'>No parts selected.</p>}{items.map(i=><div key={i.id} className='py-2 border-b border-slate-800'><p className='text-sm'>{i.name}</p><p>${i.current_price}</p><button className='text-xs text-red-400' onClick={()=>onRemove(i.id)}>Remove</button></div>)}<p className='mt-3 font-bold'>Total: ${total.toFixed(2)}</p><button className='mt-2 w-full bg-slate-700' onClick={onClear}>Clear Build</button></aside>}
